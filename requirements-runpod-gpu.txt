# GPU pod deps — do NOT install CPU torch (image already has CUDA 11.8 torch).
sentence-transformers>=3.0,<4.0
transformers>=4.40,<5.0
datasets>=2.18,<4.0
accelerate>=0.30
optimum[onnxruntime]>=1.20
rank-bm25>=0.2
huggingface_hub>=0.23
